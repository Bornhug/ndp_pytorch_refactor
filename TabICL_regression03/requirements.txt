# Recommended runtime for TabICL_regression03.
#
# Python is not installed by requirements.txt. Use Python 3.11.15 if you want
# torch.compile support across RTX 5090/4090/4070/2080 Ti.
#
# CUDA 12.9 PyTorch wheels require a recent NVIDIA driver:
# - Linux/WSL host driver >= 575.51.03
# - Windows host driver >= 576.02
#
# The TabICL source repo is still required separately by config.training.tabicl_repo.
#
# For torch.compile on CUDA, requirements.txt is not enough because the CUDA
# compiler should come from conda in this WSL setup:
#   conda install -y -c nvidia cuda-nvcc=12.9.86

--extra-index-url https://download.pytorch.org/whl/cu129

torch==2.8.0+cu129
numpy==2.3.3
pandas==2.3.3
scikit-learn==1.7.1
scipy==1.15.3
joblib==1.5.1
xgboost==3.1.2
transformers==4.57.3
einops==0.8.1
psutil==7.2.1
wandb==0.23.0
tqdm==4.67.1
huggingface-hub==0.36.0
matplotlib==3.10.5

# Optional, only needed to run TabICL_regression03/tests:
# pytest
